``` python
arr = [int(input()) for i in range(9)]
total = sum(arr)
flag = False
        
for i in range(9):
    for j in range(9):
            if i == j:
                continue
            if total - arr[i] - arr[j] == 100:
                flag = True
                for k in range(9):
                    if k == i or k == j:
                        continue
                    print(arr[k])
                            
            if flag:
                break
```

